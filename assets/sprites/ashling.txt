# name: The Ashling
# size: 3x3
# anchor: center
# fps: 8
# colour: w
#
# She sheds embers as she walks -- one warm cell, alternating feet.
#
# Those embers used to be `.` and `,`. `.` is retired game-wide (at a glance it
# is `·`, and `·` is XP); `,` is the Grave Rat's tail. Both are now `'`, which is
# the ember glyph in juice.tsv, which is exactly what they are. Player sprites
# may wear the Warden's `@ / \ |` -- they ARE the Warden -- but no sprite on the
# field gets to borrow the mote or a rat.
--- art ---
 &
/|\
/ '
--- mask ---
 W
www
w R
--- art ---
 &
\|/
' |
--- mask ---
 W
www
r w
