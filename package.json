{
  "name": "ascii-vampire-survivors",
  "version": "0.1.0",
  "private": true,
  "description": "A Vampire-Survivors-style ASCII game that runs in your terminal.",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node src/tools/build.ts && node src/tools/serve-static.ts --open",
    "dev": "node src/serve.ts",
    "build": "node src/tools/build.ts",
    "preview": "node src/tools/serve-static.ts",
    "tui": "node src/main.ts",
    "tui:watch": "node src/main.ts --watch",
    "art": "node src/main.ts --preview",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.web.json --noEmit",
    "test": "node --test src/tests/*.test.ts",
    "bench": "node src/bench.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
